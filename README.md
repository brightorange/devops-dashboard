# DevOps Dashboard

DevOps Dashboard is a simple command-line utility for checking basic system and operations information from one menu.

## Modules

- `sysinfo`: Displays hostname, current user, and working directory.
- `logcheck`: Checks logs for issues (current project module).
- `tasklist`: Manages a basic task list from the dashboard menu.
